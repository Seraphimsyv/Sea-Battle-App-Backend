export enum EnumGameStatus {
  PREPARE, PROCESSING, FINISHED, ERROR
};

export enum EnumOpponentStatus {
  NOT_CONNECTED, PREPARATION, PREPARED, IN_GAME
}

export enum EnumPlayerPlaygroundStatus {
  EDITABLE, READY
};

export enum EnumShipStatus {
  DESTROYED, NOT_DAMAGED
};

export enum EnumShotStatus {
  PAST, HIT
};

export enum EnumPlayerTurnStatus {
  OpponentTurn, PlayerTurn
};